---
"mattpocock-skills": minor
---

Add an adapted, model-invoked `ponytail` skill. It runs a strict complexity gate before coding and delivery so implementations stay small without dropping accepted scope, project contracts, tests, or verification, while preserving approved UX and design fidelity with proportional visual acceptance.
